# ============================================================================
# UIP - Urban Intelligence Platform
# Copyright (c) 2024-2025 UIP Team. All rights reserved.
# https://github.com/UIP-Urban-Intelligence-Platform/UIP-Urban_Intelligence_Platform
#
# SPDX-License-Identifier: MIT
# ============================================================================
# File: .github/workflows/deploy.yml
# Module: CI/CD Pipeline - Auto Deploy to Ubuntu VPS
# Author: Nguyen Dinh Anh Tuan
# Created: 2025-12-08
# Version: 1.0.0
# License: MIT
#
# Description:
#   Automated CI/CD pipeline for deploying to Ubuntu VPS
#   - Chức năng 1: Tự động đẩy code lên server khi có thay đổi trên GitHub
#   - Chức năng 2: Tự động khởi chạy lại project sau khi deploy
#
# Server Info:
#   - IP:
#   - User:
#   - Path: /home/deepminds/builder-layer-end
#
# Prerequisites:
#   1. Tạo các secrets trong GitHub Repository Settings > Secrets:
#      - VPS_HOST:
#      - VPS_USER:
#      - VPS_SSH_KEY: Private SSH key để kết nối VPS
#      - VPS_PORT: 22
#      - VPS_PASSWORD: Password SSH (nếu dùng password thay vì key)
#
#   2. Trên VPS Ubuntu đã có sẵn:
#      - Python 3.x
#      - Node.js
#      - Docker & Docker Compose
#
# Usage:
#   - Push code lên branch main sẽ tự động trigger deploy
#   - Có thể chạy thủ công từ Actions tab
# ============================================================================

name: Deploy to Ubuntu VPS

on:
    # Trigger khi push lên branch main hoặc tuan
    push:
        branches:
            - main
            - tuan

    # Cho phép chạy thủ công từ GitHub Actions tab
    workflow_dispatch:
        inputs:
            skip_orchestrator:
                description: "Skip running orchestrator and CV sync"
                required: false
                default: "false"
                type: boolean
            full_restart:
                description: "Full restart (stop all services first)"
                required: false
                default: "false"
                type: boolean

env:
    PROJECT_NAME: uip-urban-intelligence-platform

jobs:
    # ===========================================================================
    # Job 1: Deploy code to VPS
    # ===========================================================================
    deploy:
        name: 🚀 Deploy to VPS
        runs-on: ubuntu-latest

        steps:
            # Bước 1: Checkout code
            - name: 📥 Checkout code
              uses: actions/checkout@v4
              with:
                  fetch-depth: 0 # Fetch full history for proper sync

            # Bước 2: Install sshpass (để dùng password SSH)
            - name: 🔧 Install sshpass
              run: sudo apt-get install -y sshpass

            # Bước 3: Deploy code lên VPS bằng rsync
            - name: 📤 Deploy code to VPS
              env:
                  VPS_HOST: ${{ secrets.VPS_HOST }}
                  VPS_USER: ${{ secrets.VPS_USER }}
                  VPS_PASSWORD: ${{ secrets.VPS_PASSWORD }}
                  VPS_PORT: ${{ secrets.VPS_PORT }}
              run: |
                  echo "🚀 Deploying to VPS: $VPS_HOST"

                  # Disable strict host key checking
                  mkdir -p ~/.ssh
                  echo "StrictHostKeyChecking no" >> ~/.ssh/config

                  # Sync code using rsync với sshpass (password authentication)
                  sshpass -p "$VPS_PASSWORD" rsync -avz --delete \
                    --exclude '.git' \
                    --exclude '.venv' \
                    --exclude 'node_modules' \
                    --exclude '__pycache__' \
                    --exclude '*.pyc' \
                    --exclude '.env' \
                    --exclude 'logs/*' \
                    --exclude 'data/cache/*' \
                    --exclude 'test_output/*' \
                    --exclude 'htmlcov' \
                    --exclude 'coverage.xml' \
                    --exclude '.pytest_cache' \
                    --exclude 'assets/models/*.pth' \
                    -e "ssh -p ${VPS_PORT:-22} -o StrictHostKeyChecking=no" \
                    ./ ${VPS_USER}@${VPS_HOST}:/home/${VPS_USER}/builder-layer-end/

                  echo "✅ Code deployed successfully!"

            # Bước 4: Cài đặt dependencies và khởi chạy project
            - name: 🔧 Setup and Start Project
              env:
                  VPS_HOST: ${{ secrets.VPS_HOST }}
                  VPS_USER: ${{ secrets.VPS_USER }}
                  VPS_PASSWORD: ${{ secrets.VPS_PASSWORD }}
                  VPS_PORT: ${{ secrets.VPS_PORT }}
                  
                  # ============ ROOT .env API Keys ============
                  # External APIs
                  OPENWEATHERMAP_API_KEY: ${{ secrets.OPENWEATHERMAP_API_KEY }}
                  OPENAQ_API_KEY: ${{ secrets.OPENAQ_API_KEY }}
                  GEONAMES_USERNAME: ${{ secrets.GEONAMES_USERNAME }}
                  # Database passwords
                  NEO4J_PASSWORD: ${{ secrets.NEO4J_PASSWORD }}
                  FUSEKI_PASSWORD: ${{ secrets.FUSEKI_PASSWORD }}
                  POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
                  MONGODB_PASSWORD: ${{ secrets.MONGODB_PASSWORD }}
                  REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
                  # Security
                  SECRET_KEY: ${{ secrets.SECRET_KEY }}
                  
                  # ============ BACKEND .env API Keys ============
                  # Traffic Maestro Agent
                  TICKETMASTER_API_KEY: ${{ secrets.TICKETMASTER_API_KEY }}
                  MAPBOX_API_KEY: ${{ secrets.MAPBOX_API_KEY }}
                  # Graph Investigator Agent
                  GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
                  TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
                  # Eco-Twin Agent
                  OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
                  GOOGLE_CALENDAR_API_KEY: ${{ secrets.GOOGLE_CALENDAR_API_KEY }}
                  GOOGLE_SEARCH_ENGINE_ID: ${{ secrets.GOOGLE_SEARCH_ENGINE_ID }}
                  GOOGLE_SEARCH_API_KEY: ${{ secrets.GOOGLE_SEARCH_API_KEY }}
                  
              run: |
                  sshpass -p "$VPS_PASSWORD" ssh -p ${VPS_PORT:-22} -o StrictHostKeyChecking=no ${VPS_USER}@${VPS_HOST} << ENDSSH
                    set -e
                    
                    echo "📁 Navigating to project directory..."
                    cd /home/\$USER/builder-layer-end
                    
                    echo "============================================"
                    echo "  UIP - Urban Intelligence Platform"
                    echo "  Auto Deployment from GitHub Actions"
                    echo "============================================"
                    
                    # ==== Step 1: Create directories ====
                    echo "📁 Creating directories..."
                    mkdir -p logs data data/cache data/rdf reports test_output assets/models
                    
                    # ==== Step 2: Setup environment files with API keys ====
                    echo "📄 Setting up environment files..."
                    
                    # Function to update env var in a specific file
                    update_env_var() {
                      local file=\$1
                      local key=\$2
                      local value=\$3
                      if [ -n "\$value" ]; then
                        if grep -q "^\$key=" "\$file" 2>/dev/null; then
                          sed -i "s|^\$key=.*|\$key=\$value|" "\$file"
                        else
                          echo "\$key=\$value" >> "\$file"
                        fi
                      fi
                    }
                    
                    # ========== Setup ROOT .env ==========
                    echo "📄 Configuring root .env..."
                    if [ ! -f ".env" ]; then
                      [ -f ".env.example" ] && cp .env.example .env || touch .env
                    fi
                    
                    # External APIs
                    update_env_var ".env" "OPENWEATHERMAP_API_KEY" "${OPENWEATHERMAP_API_KEY}"
                    update_env_var ".env" "OPENAQ_API_KEY" "${OPENAQ_API_KEY}"
                    update_env_var ".env" "GEONAMES_USERNAME" "${GEONAMES_USERNAME}"
                    
                    # Database passwords
                    update_env_var ".env" "NEO4J_PASSWORD" "${NEO4J_PASSWORD}"
                    update_env_var ".env" "FUSEKI_PASSWORD" "${FUSEKI_PASSWORD}"
                    update_env_var ".env" "POSTGRES_PASSWORD" "${POSTGRES_PASSWORD}"
                    update_env_var ".env" "MONGODB_PASSWORD" "${MONGODB_PASSWORD}"
                    update_env_var ".env" "REDIS_PASSWORD" "${REDIS_PASSWORD}"
                    
                    # Security
                    update_env_var ".env" "SECRET_KEY" "${SECRET_KEY}"
                    
                    echo "✅ Root .env configured"
                    
                    # ========== Setup BACKEND .env ==========
                    echo "📄 Configuring backend .env..."
                    BACKEND_ENV="apps/traffic-web-app/backend/.env"
                    if [ ! -f "\$BACKEND_ENV" ]; then
                      [ -f "apps/traffic-web-app/backend/.env.example" ] && cp apps/traffic-web-app/backend/.env.example "\$BACKEND_ENV" || touch "\$BACKEND_ENV"
                    fi
                    
                    # Database (same as root)
                    update_env_var "\$BACKEND_ENV" "NEO4J_PASSWORD" "${NEO4J_PASSWORD}"
                    update_env_var "\$BACKEND_ENV" "FUSEKI_PASSWORD" "${FUSEKI_PASSWORD}"
                    update_env_var "\$BACKEND_ENV" "POSTGRES_PASSWORD" "${POSTGRES_PASSWORD}"
                    
                    # Traffic Maestro Agent APIs
                    update_env_var "\$BACKEND_ENV" "TICKETMASTER_API_KEY" "${TICKETMASTER_API_KEY}"
                    update_env_var "\$BACKEND_ENV" "MAPBOX_API_KEY" "${MAPBOX_API_KEY}"
                    
                    # Graph Investigator Agent APIs
                    update_env_var "\$BACKEND_ENV" "GEMINI_API_KEY" "${GEMINI_API_KEY}"
                    update_env_var "\$BACKEND_ENV" "TAVILY_API_KEY" "${TAVILY_API_KEY}"
                    
                    # Eco-Twin Agent APIs
                    update_env_var "\$BACKEND_ENV" "OPENWEATHER_API_KEY" "${OPENWEATHER_API_KEY}"
                    update_env_var "\$BACKEND_ENV" "GOOGLE_CALENDAR_API_KEY" "${GOOGLE_CALENDAR_API_KEY}"
                    update_env_var "\$BACKEND_ENV" "GOOGLE_SEARCH_ENGINE_ID" "${GOOGLE_SEARCH_ENGINE_ID}"
                    update_env_var "\$BACKEND_ENV" "GOOGLE_SEARCH_API_KEY" "${GOOGLE_SEARCH_API_KEY}"
                    
                    echo "✅ Backend .env configured"
                    
                    # ========== Setup FRONTEND .env ==========
                    [ ! -f "apps/traffic-web-app/frontend/.env" ] && [ -f "apps/traffic-web-app/frontend/.env.example" ] && cp apps/traffic-web-app/frontend/.env.example apps/traffic-web-app/frontend/.env || true
                    
                    echo "✅ All environment variables configured"
                    
                    # ==== Step 3: Setup Python environment ====
                    echo "🐍 Setting up Python environment..."
                    [ ! -d ".venv" ] && python3 -m venv .venv
                    source .venv/bin/activate
                    pip install --upgrade pip setuptools wheel -q
                    pip install -r requirements/base.txt -q 2>/dev/null || pip install -r requirements.txt -q 2>/dev/null || true
                    pip show yolox > /dev/null 2>&1 || pip install git+https://github.com/Megvii-BaseDetection/YOLOX.git -q 2>/dev/null || echo "YOLOX install skipped"
                    
                    # ==== Step 4: Setup Node.js dependencies ====
                    echo "📦 Setting up Node.js dependencies..."
                    [ -d "apps/traffic-web-app/backend" ] && (cd apps/traffic-web-app/backend && npm install --silent 2>/dev/null || npm install)
                    [ -d "apps/traffic-web-app/frontend" ] && (cd apps/traffic-web-app/frontend && npm install --silent 2>/dev/null || npm install)
                    
                    # ==== Step 5: Stop existing services ====
                    echo "🛑 Stopping existing services..."
                    pkill -f "main.py" 2>/dev/null || true
                    pkill -f "orchestrator.py" 2>/dev/null || true
                    pkill -f "run_cv_and_sync.py" 2>/dev/null || true
                    pkill -f "npm run dev" 2>/dev/null || true
                    pkill -f "vite" 2>/dev/null || true
                    pm2 stop all 2>/dev/null || true
                    sleep 3
                    
                    # ==== Step 6: Start Docker services ====
                    echo "🐳 Starting Docker infrastructure..."
                    if docker compose version > /dev/null 2>&1; then
                      COMPOSE_CMD="docker compose"
                    else
                      COMPOSE_CMD="docker-compose"
                    fi
                    \$COMPOSE_CMD up -d neo4j fuseki redis mongodb postgres kafka 2>&1 || true
                    echo "Waiting for databases (60s)..."
                    sleep 60
                    \$COMPOSE_CMD up -d stellio-api-gateway search-service subscription-service 2>&1 || true
                    echo "Waiting for Stellio (30s)..."
                    sleep 30
                    
                    # ==== Step 7: Run initial scripts (QUAN TRỌNG!) ====
                    echo "🚀 Running initial scripts..."
                    source .venv/bin/activate
                    
                    echo "Step 7a: Running orchestrator.py..."
                    timeout 300 python3 orchestrator.py &
                    sleep 15
                    
                    echo "Step 7b: Running run_cv_and_sync.py (initial sync)..."
                    timeout 600 python3 scripts/pipeline/run_cv_and_sync.py --runs 1 --delay 0 2>/dev/null || true
                    
                    # ==== Step 8: Start main services (continuous mode) ====
                    echo "🎯 Starting main services..."
                    
                    # Python backend
                    nohup python3 main.py --run-orchestrator-now > logs/main_py.log 2>&1 &
                    echo \$! > logs/main_py.pid
                    echo "✅ Python backend started"
                    
                    # CV Analysis Pipeline (continuous)
                    nohup python3 scripts/pipeline/run_cv_and_sync.py --runs 9999 --delay 60 > logs/cv_sync.log 2>&1 &
                    echo \$! > logs/cv_sync.pid
                    echo "✅ CV Analysis Pipeline started"
                    
                    # TypeScript Backend
                    cd apps/traffic-web-app/backend
                    nohup npm run dev > ../../../logs/ts_backend.log 2>&1 &
                    echo \$! > ../../../logs/ts_backend.pid
                    cd /home/\$USER/builder-layer-end
                    echo "✅ TypeScript backend started"
                    
                    # React Frontend
                    cd apps/traffic-web-app/frontend
                    nohup npm run dev -- --host 0.0.0.0 > ../../../logs/frontend.log 2>&1 &
                    echo \$! > ../../../logs/frontend.pid
                    cd /home/\$USER/builder-layer-end
                    echo "✅ React frontend started"
                    
                    echo ""
                    echo "============================================"
                    echo "  ✅ DEPLOYMENT COMPLETED!"
                    echo "============================================"
                    echo "Services running:"
                    echo "  🌐 Main URL:  http://54.179.155.70 (via Nginx)"
                    echo "  - Frontend:   http://54.179.155.70:5173"
                    echo "  - Backend:    http://54.179.155.70:5000"
                    echo "  - FastAPI:    http://54.179.155.70:8001"
                    echo "  - Stellio:    http://54.179.155.70:8080"
                    echo "  - Neo4j:      http://54.179.155.70:7474"
                    echo "============================================"
                  ENDSSH

            # Bước 5: Health check
            - name: 🏥 Health Check
              env:
                  VPS_HOST: ${{ secrets.VPS_HOST }}
                  VPS_USER: ${{ secrets.VPS_USER }}
                  VPS_PASSWORD: ${{ secrets.VPS_PASSWORD }}
                  VPS_PORT: ${{ secrets.VPS_PORT }}
              run: |
                  echo "🏥 Running health check..."
                  sleep 30

                  sshpass -p "$VPS_PASSWORD" ssh -p ${VPS_PORT:-22} -o StrictHostKeyChecking=no ${VPS_USER}@${VPS_HOST} << 'ENDSSH'
                    cd /home/$USER/builder-layer-end
                    
                    echo "Checking services..."
                    
                    # Check Docker containers
                    if docker ps --format '{{.Names}}' | grep -q "test-"; then
                      echo "✅ Docker containers are running"
                    else
                      echo "⚠️ Some Docker containers may not be running"
                    fi
                    
                    # Check Python processes
                    if pgrep -f "main.py" > /dev/null; then
                      echo "✅ Main Python service is running"
                    else
                      echo "⚠️ Main Python service is not running"
                    fi
                    
                    # Check CV Analysis
                    if pgrep -f "run_cv_and_sync.py" > /dev/null; then
                      echo "✅ CV Analysis pipeline is running"
                    else
                      echo "⚠️ CV Analysis pipeline is not running"
                    fi
                    
                    # Show running processes
                    echo ""
                    echo "📋 Running processes:"
                    ps aux | grep -E "main.py|run_cv_and_sync|node" | grep -v grep || echo "No processes found"
                  ENDSSH

    # ===========================================================================
    # Job 2: Deploy Docusaurus Docs to GitHub Pages
    # ===========================================================================
    deploy-docs:
        name: 📚 Deploy Docs to GitHub Pages
        runs-on: ubuntu-latest
        needs: deploy

        # Grant GITHUB_TOKEN the permissions required to make a Pages deployment
        permissions:
            pages: write
            id-token: write
            contents: read

        # Deploy to the github-pages environment
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}

        steps:
            # Bước 1: Checkout code
            - name: 📥 Checkout code
              uses: actions/checkout@v4

            # Bước 2: Setup Node.js
            - name: 📦 Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "20"
                  cache: "npm"
                  cache-dependency-path: apps/traffic-web-app/frontend/docs/package-lock.json

            # Bước 3: Install dependencies
            - name: 📦 Install dependencies
              working-directory: apps/traffic-web-app/frontend/docs
              run: npm ci || npm install

            # Bước 4: Build Docusaurus
            - name: 🔨 Build Docusaurus
              working-directory: apps/traffic-web-app/frontend/docs
              run: npm run build

            # Bước 5: Setup Pages
            - name: 📄 Setup Pages
              uses: actions/configure-pages@v4

            # Bước 6: Upload artifact
            - name: 📤 Upload artifact
              uses: actions/upload-pages-artifact@v3
              with:
                  path: apps/traffic-web-app/frontend/docs/build

            # Bước 7: Deploy to GitHub Pages
            - name: 🚀 Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v4

    # ===========================================================================
    # Job 3: Notify on completion (optional)
    # ===========================================================================
    notify:
        name: 📢 Notify
        runs-on: ubuntu-latest
        needs: [deploy, deploy-docs]
        if: always()

        steps:
            - name: 📢 Send notification
              run: |
                  echo "============================================"
                  echo "  📢 Deployment Summary"
                  echo "============================================"
                  
                  if [ "${{ needs.deploy.result }}" == "success" ]; then
                    echo "✅ VPS Deployment: SUCCESS"
                    echo "🌐 Project deployed to: ${{ secrets.VPS_HOST }}"
                  else
                    echo "❌ VPS Deployment: FAILED"
                  fi
                  
                  if [ "${{ needs.deploy-docs.result }}" == "success" ]; then
                    echo "✅ Docs Deployment: SUCCESS"
                    echo "📚 Docs URL: https://uip-urban-intelligence-platform.github.io/UIP-Urban_Intelligence_Platform/"
                  else
                    echo "⚠️ Docs Deployment: FAILED or SKIPPED"
                  fi
                  
                  echo "============================================"
